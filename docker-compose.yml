version: '3.8'

services:

  hackathon-db:
    image: pgvector/pgvector:pg16
    restart: always
    environment:
      POSTGRES_DB: hackathon
      POSTGRES_USER: hackathon
      POSTGRES_PASSWORD: uskgh
    ports:
      - "5444:5432"
